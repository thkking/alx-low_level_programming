#include <stdio.h>
#include "main.h"

/**
 * main - Print the largest prime factor
 * Return: 0 if success
 */

int main(void)
{
	long x;
	long maxf;
	double number = 612852475143

		for (x = 1; x <= square; x++)
		{
			if (number % x == 0)
			{
				maxf = number / x;
			}
		}
	printf("%d\n", maxf);
	return (0);
}
